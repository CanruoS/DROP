% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drop.latex.R
\name{drop.latex}
\alias{drop.latex}
\title{Generate LaTeX Tables from DROP Simulation Results}
\usage{
drop.latex(file_path, output_file = "drop_tables.tex",
           metrics = c("F1", "MCC"), show_sd = TRUE, digits = 3,
           p_values = NULL, scenarios = NULL,
           landscape = TRUE)
}
\arguments{
\item{file_path}{Character string (file path), list with 'summary' component, or data frame.
Can be: (1) Path to RDS/Excel file, (2) Result object from drop.simulate()/drop.merge(),
or (3) Summary data frame directly.}

\item{output_file}{Character. Path for the output LaTeX file. Default: "drop_tables.tex"}

\item{metrics}{Character vector. Metrics to include in the table. 
Default: c("F1", "MCC"). Available options: "F1", "MCC", "Precision", "Recall", "Specificity", "Time", "Success Rate".
You can select any combination, e.g., c("Precision", "Recall"), "Success Rate", or just "F1".}

\item{show_sd}{Logical. Whether to show standard deviations in parentheses. Default: TRUE}

\item{digits}{Integer. Number of decimal places to display. Default: 3}

\item{p_values}{Numeric vector. Which p values to generate tables for. 
If NULL (default), uses all p values in the data.}

\item{scenarios}{Character vector. Which contamination scenarios to include.
If NULL (default), uses all scenarios in the data.}

\item{landscape}{Logical. Whether to use landscape mode. Default: TRUE}
}
\value{
Invisibly returns the summary data frame used to generate tables.
}
\description{
This function reads simulation results from RDS or Excel files, or accepts pre-loaded
data frames, and creates formatted LaTeX tables showing performance metrics with 
standard deviations across different contamination scenarios. Table titles are 
dynamically generated based on selected metrics.
}
\details{
The function expects the input to be one of:
\itemize{
  \item A file path (character string) to an RDS or Excel file
  \item A list object with a 'summary' component (from drop.simulate()/drop.merge())
  \item A data frame directly (the summary data frame)
}

The summary data frame should contain columns including:
\itemize{
  \item method_name
  \item n (sample size)
  \item p (dimension)
  \item graph_type
  \item contamination_scenario
  \item F1_mean, F1_sd (optional)
  \item MCC_mean, MCC_sd (optional)
  \item Precision_mean, Precision_sd (optional)
  \item Recall_mean, Recall_sd (optional)
  \item Specificity_mean, Specificity_sd (optional)
  \item time_mean, time_sd (optional)
  \item success_rate (optional, no SD)
}

The generated LaTeX tables include:
\itemize{
  \item Dynamic titles based on selected metrics (e.g., "F1 and MCC scores for p=10")
  \item Separate tables for each p value and contamination scenario
  \item Methods in rows, graph types in columns
  \item Multiple sample sizes grouped with midrules
  \item Mean values with standard deviations in parentheses (if show_sd = TRUE)
  \item Success Rate shown as single values without SD
  \item Metric headers centered over their respective graph type columns
  \item Professional formatting with booktabs package
  \item Customizable decimal places via digits parameter
}
}
\examples{
\dontrun{
# First, run a simulation experiment
sim_result <- drop.simulate(
  n = 100,
  p = 10,
  graph_type = "hub",
  g = 10,
  contamination_scenario = "clean",
  simulation_times = 3,
  methods = c("DROP", "HUGE_Glasso"),
  seed = 789
)

# Generate LaTeX tables with F1 and MCC metrics
drop.latex(sim_result, output_file = "results.tex", 
           metrics = c("F1", "MCC"))
}
}
\seealso{
\code{\link{drop.simulate}}, \code{\link{drop.merge}}, \code{\link{drop.excel}}
}
